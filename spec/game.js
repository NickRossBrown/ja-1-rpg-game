import "character" from './../src/game.js';

desvcribe ('Character', function () {

  it ('should test whether a character is dead or alive', function ({
    const character = new Character(name, html, css, jquery, javascript);
    expect(true || false)
  }))
  it ('should test whether a character is dead or alive', function ({
    const character = new Character(name, html, css, jquery, javascript);
    expect(true || false)
  }))


})
